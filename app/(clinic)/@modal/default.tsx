// A slot has to render something for every URL. default.tsx is the fallback
// when @modal has no matching page — which is most of the time: no modal.
export default function Default() {
  return null;
}
